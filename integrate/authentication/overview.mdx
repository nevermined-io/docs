---
title: "Authentication & Identity"
description: "How agents and humans authenticate to the Nevermined API — one bearer token, and the standards-based ceremonies to obtain it with or without a browser."
icon: "key"
related:
  - For AI agents — read this first: /docs/integrate/authentication/for-agents
  - Get a Nevermined API key: /docs/agents-guide/get-api-key
  - Device flow (RFC 8628): /docs/integrate/authentication/device-flow
  - OAuth 2.1 Authorization Code: /docs/integrate/authentication/oauth-authorization-code
---

<Note>
**Are you an AI agent, or building one?**

Start with [Authentication for AI agents](/integrate/authentication/for-agents) — it points to the machine-readable manifest an agent can fetch and follow on its own.
</Note>

Every agent-callable Nevermined endpoint is authenticated by a single bearer token. The full mechanics — the exact header, the per-tier base URLs and key prefixes (`sandbox:…` / `live:…`), what a Nevermined key actually is, and how to read a `401` — are the canonical, always-current [`auth.md`](https://nevermined.app/auth.md) manifest. This page covers the decisions around that ceremony: **which path to use, which credential each yields, and the per-tier discovery quirk.**

## Which path do I use?

<CardGroup cols={3}>
  <Card title="SDK key" icon="terminal">
    You can run an SDK and manage your own identity. The simplest path — the SDK mints a key directly. See [Get a Nevermined API key](/agents-guide/get-api-key).
  </Card>
  <Card title="Device flow (RFC 8628)" icon="mobile-screen">
    You're a headless agent with no browser, but a human can approve for you. Approval is collected without a browser on your side — typically ending in an **NVM API key** bound to the approver. See [Device flow](/integrate/authentication/device-flow).
  </Card>
  <Card title="Authorization Code + PKCE" icon="browser">
    You're a browser-based MCP client (Cursor, Claude). The user approves in a browser — to obtain an **x402 payment permission** for a specific agent, or, with `scope=commerce`, an **NVM API key** plus a spending cap an AI assistant (Claude, ChatGPT) pays external services with. See [Authorization Code](/integrate/authentication/oauth-authorization-code).
  </Card>
</CardGroup>

<Note>
**The credential you receive is chosen by the requested `resource`, not by the grant.** A `resource` (RFC 8707 audience) matching this API's host mints an **NVM API key**; any other resource mints an **x402 payment permission**. An account-level consent — `commerce` or `account_access` — always yields an API key, whatever `resource` says; with no `resource`, a delegation-backed agent-specific binding yields an x402 permission and a plan-only binding yields an API key. The two grants differ only in *how the human approves* — Authorization Code in a browser, the device flow without one — so pick the grant by your environment and the `resource` by the credential you need.
</Note>

## Discovery

Everything the ceremony needs is advertised in standards-based OAuth metadata — the RFC 8414 authorization-server document, the RFC 9728 protected-resource document, and the JWKS. See [`auth.md`](https://nevermined.app/auth.md) for the endpoint list and what each returns. If a capability is absent from these documents, it does not exist — don't infer it.

### Each tier is its own issuer, and its authorize URL says so

Sandbox and Live are separate authorization servers with separate discovery documents (`https://api.sandbox.nevermined.app/.well-known/oauth-authorization-server` and `https://api.live.nevermined.app/…`), but both send the human to the same web app for consent. That app cannot tell from a bare `/oauth/authorize` link which issuer it is acting for, so **the human-facing endpoints carry the tier as a query parameter**:

```json
{
  "issuer": "https://api.sandbox.nevermined.app",
  "authorization_endpoint": "https://nevermined.app/oauth/authorize?network=sandbox",
  "token_endpoint": "https://api.sandbox.nevermined.app/oauth/token",
  "authorization_response_iss_parameter_supported": true
}
```

`network` is `sandbox` or `live` — the same parameter the embeddable widgets take, and distinct from the x402 request body's `network` field, which names the settlement rail (`eip155:<chainId>`, or `stripe` / `braintree` / `visa` — [details](/development-guide/nevermined-x402#paymentrequired-response-402)), never a tier. The device flow's `verification_uri` carries it too. **Keep the query string** when you append your own parameters (RFC 6749 §3.1 requires it). Not every client does: the official Python MCP client (`mcp`, every release through 2.2.0) concatenates a second `?`, producing `…?network=sandbox?response_type=code&…` — the consent page tolerates that shape, taking the tier from the head and recovering the one swallowed parameter, so such clients still work; but do not rely on it for a client you control — extend the query with `&`. A discovery-driven client gets the right value for free; if you configure an OAuth provider **by hand** — a connector's "authorization URL" form field, say — copy the endpoint from the tier's discovery document, query string included. **The endpoint must end up as `…/oauth/authorize?network=sandbox` or `…?network=live`; if the document you fetch carries no `network`, append it yourself.** An authorize link without `network` is served on whichever tier the user's browser last used (Live by default), and a connector registered only on Sandbox is then refused as *"Connector not authorized"* until the user switches manually.

### The authorization response names its issuer (RFC 9207)

Each tier advertises `authorization_response_iss_parameter_supported: true`, and the consent page appends **`iss`** to every redirect back to your `redirect_uri` — the `?code=…&state=…` success and the `?error=…&state=…` denial alike. Its value is exactly the `issuer` of the discovery document you resolved (`https://api.sandbox.nevermined.app` or `https://api.live.nevermined.app`). Compare the two by plain string equality and reject a response whose `iss` does not match, or that has none: that is what stops a code minted by one issuer being presented as another's (the mix-up attack RFC 9207 exists for), and it is also what lets a client that registers itself through its metadata document (ChatGPT, for one) present a stable identity rather than a per-connection one. The device flow has no authorization response, so nothing there carries `iss`.

## What needs a human

Three things require a person in the loop — card enrolment (the card is tokenized in-browser, never reaching Nevermined), first-account creation, and creating a `provider=visa` delegation (a per-delegation FIDO/passkey device-binding ceremony). You can *use* an existing Visa delegation, and Stripe, Braintree, and crypto delegations you create yourself. [`auth.md`](https://nevermined.app/auth.md) documents each, and why, in full.

## Next steps

<CardGroup cols={2}>
  <Card title="Authentication for AI agents" icon="robot" href="/integrate/authentication/for-agents">
    The machine-readable manifest an agent fetches and follows.
  </Card>
  <Card title="Device flow (RFC 8628)" icon="mobile-screen" href="/integrate/authentication/device-flow">
    Get an API key with a human's approval, no browser on your side.
  </Card>
  <Card title="Authorization Code + PKCE" icon="browser" href="/integrate/authentication/oauth-authorization-code">
    Browser-based MCP clients paying for a specific agent — and AI assistants obtaining a commerce grant.
  </Card>
  <Card title="Connected agents" icon="link" href="/integrate/authentication/connections">
    List and revoke the agents a user has authorized.
  </Card>
</CardGroup>
